// // A typed version of Object.assign, expecting the new object to have properties from the current object
export function objectAssign(theObject, newProperties) {
    return Object.assign(theObject, newProperties);
}
